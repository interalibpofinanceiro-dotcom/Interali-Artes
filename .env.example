# Chave da OpenAI usada pelos agentes CrewAI (Onboarding, Copywriter, QA).
# Se deixada em branco, o sistema roda em MODO SIMULADO (respostas heurísticas,
# sem chamar LLM) para permitir testar o MVP imediatamente.
OPENAI_API_KEY=

# Modelo usado pelos agentes quando OPENAI_API_KEY estiver configurada.
OPENAI_MODEL_NAME=gpt-4o-mini

# Banco de dados. Padrão: SQLite local (arquivo interali.db na raiz do projeto).
# Para Postgres em produção, use algo como:
# DATABASE_URL=postgresql+psycopg2://usuario:senha@host:5432/interali
DATABASE_URL=sqlite:///interali.db
